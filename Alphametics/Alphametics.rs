use std::collections::HashMap;

pub fn solve(input: &str) -> Option<HashMap<char, u8>>
{
    unimplemented!("Solve the alphametic {input:?}")
    //My idea is to do it using binary
    //We _can_ do it that way
    //Whatever number of letters we have
    //Lets call it n
    //Each of them can only have 9 possibilites of less than one
    //I think I'm on to something here
}
